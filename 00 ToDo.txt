1. Update the E2E tests and readme to the current state of the project and be able to test everything. 
2. Create a full documentation of every screen and feature including screenshots and structure this as a repo inside the folder docs.
3. For Linux, only build a flatpack for now.


1. In autocode, prefill the coding prompt.
2. In the settings, rename "QualCoder v4.0 (dev) - rewrite of QualCoder" to "QCnext - A Rework of QualCoder". In the very bottom right of the window, change the version number to 0.1.0
3. a) In the AI menu, the API base URL of LMStudio is wrong (must be http://127.0.0.1:1234). 
b) Also, "2026-08-12 18:09:53 [DEBUG] Received request: GET to /models 2026-08-12 18:09:53 [ERROR] Unexpected endpoint or method. (GET /models). Returning 200 anyway". 
c) Model polling still fails; search online to correct this for every method. 
d) the error with Gemini persists.
